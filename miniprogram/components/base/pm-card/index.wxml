<view class="pm-card pm-card--{{status}} {{clickable ? 'pm-card--clickable' : ''}}" bindtap="handleTap">
  <view class="pm-card__header">{{title}}</view>
  <view class="pm-card__body">{{description}}</view>
</view>
